<div class="container">
	<% if(success && success.length) { %>
	<div class="alert-success my-3 p-3" role="alert">
		<%= success %>
	</div>
	<% } %>
	<% if(error && error.length) { %>
	<div class="alert-danger my-3 p-3" role="alert">
		<%= error %>
	</div>
	<% } %>
</div>